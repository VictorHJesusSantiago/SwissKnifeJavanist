server:
  port: ${PORT:8082}
spring:
  datasource:
    url: ${DATABASE_URL:jdbc:h2:file:./data/itam}
    username: ${DATABASE_USER:sa}
    password: ${DATABASE_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
